function averageLog(numX, numY, numZ) {
  var chairs = numX + numY + numZ;
  var tables = chairs / 3;
  console.log(tables);
}
average(1, 2, 3);

//function goes here
/*
Write a function that takes three parameters and computes and logs the average.
*/
